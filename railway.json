{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "DOCKERFILE"
    },
    "deploy": {
      "startCommand": "hypercorn app.main:app --bind \"[::]:$PORT\""
    }
  }